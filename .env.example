# KI3 POS does not currently require runtime environment variables.
# Firebase's browser configuration is loaded from firebase-applet-config.json.
# Never add service-account JSON, private keys, admin passwords or email/SMS
# provider secrets to this file or to any VITE_* variable (VITE_* is public).
