Place the signed Android APK at this path when it is ready:

```text
public/downloads/ki3-pos.apk
```

The public download page checks this exact file. If it exists in the deployed site, the Android APK download button becomes active automatically.

Do not put signing keys, keystore passwords, Firebase service accounts or private credentials in this folder.
